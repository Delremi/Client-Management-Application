create table Client (
    id int,
    first_name varchar(255),
    last_name varchar(255),
    username varchar(255),
    email varchar(255),
    address varchar(255),
    country_id int,
    created_by_id int
);